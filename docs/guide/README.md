# 编写原因

## 目的

- 减少代码量
- 统一代码风格
- 方便后续全局样式调整

## 已知问题/注意事项

- 本文档使用vuepress编写, 会与element-ui框架原有样式有冲突, 如仅仅是显示效果不好, 暂不调整, 项目中正式使用不会有问题

## 一些封装原则

1. 尽可能将组件的属性修改为 `Boolean` 类型, 以方便代码提示和 js 环境下代码检测
2. 尽可能将属性名与原有UI框架保持一致
3. 避免 `v-bind` 传参方式, 限制使用者对组件的控制
4. 所有组件均为照顾 PC 端, 响应式以 PC 自适应为主, 使用者不手动适配移动端
